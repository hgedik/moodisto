{
  "name": "@moodisto/validation",
  "version": "0.1.0",
  "private": true,
  "description": "Zod schemas shared by the Moodisto API and web client.",
  "main": "./dist/index.js",
  "types": "./dist/index.d.ts",
  "files": [
    "dist"
  ],
  "scripts": {
    "build": "tsc -p tsconfig.build.json",
    "typecheck": "tsc -p tsconfig.json --noEmit",
    "lint": "eslint src test --max-warnings 0",
    "test": "vitest run",
    "test:unit": "vitest run"
  },
  "dependencies": {
    "@moodisto/shared-types": "workspace:*",
    "zod": "^3.25.76"
  },
  "devDependencies": {
    "typescript": "^5.8.3",
    "vitest": "^3.1.2"
  }
}
